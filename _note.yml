cd c4-districts/ && perl-rename -v 's/province/district'/g * && cd schemas/ &&  perl-rename -v 's/province/district'/g * && cd ../dto/ && perl-rename -v 's/province/district'/g * && cd ../../
cd c5-wards/ && perl-rename -v 's/province/ward'/g * && cd schemas/ &&  perl-rename -v 's/province/ward'/g * && cd ../dto/ && perl-rename -v 's/province/ward'/g * && cd ../../
cd src/routes/common/c6-files/ && perl-rename -v 's/ward/files'/g * && cd schemas/ &&  perl-rename -v 's/ward/files'/g * && cd ../dto/ && perl-rename -v 's/ward/files'/g * && cd ../../../../../
cd src/routes/common/c6-files/ && perl-rename -v 's/file-manager/file'/g * && cd schemas/ &&  perl-rename -v 's/file-manager/file'/g * && cd ../dto/ && perl-rename -v 's/file-manager/file'/g * && cd ../../../../../
cd src/routes/common/c8-notifications/ && perl-rename -v 's/ward/notification'/g * && cd schemas/ &&  perl-rename -v 's/ward/notification'/g * && cd ../dto/ && perl-rename -v 's/ward/notification'/g * && cd ../../../../../
# UPLOAD FILE

# TODO: NEXT
# Template email
# Delete uploaded file
# Update crud file
# Add authorization
# Update authorization
# Add block select fields in api query params
# Add push fcm notification
# Add upload to s3
# Implement send otp to phone
# Send mail by sendgrid account
