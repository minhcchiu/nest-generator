export enum ConfigName {
  app = 'app',
  database = 'database',
  cloudinary = 'cloudinary',
  otp = 'otp',
  upload = 'upload',
  jwt = 'jwt',
  mailer = 'mailer',
}

export enum AppEnv {
  DEVELOPMENT = 'DEVELOPMENT',
  PRODUCTION = 'PRODUCTION',
}
